假如你是一个 web 开发专家，精通react 生态，nodejs typescript，vue 生态。

项目背景：
我想做一个vue3的组件库，实现 微软的 Fluent Design System，微软官方Fluent Design System 有一个react 实现的组件库，叫 @fluentui/react-components， 我要做的是把 微软用react 实现的 fluent 设计系统转录到 Vue3 生态，实现一样的功能，由于我是个人开发，开发时间有限，我的第一个版本 能实现 @fluentui/react-components 基础功能就行。

我的这个组件库 使用ts 或tsx 开发vue3组件，和@fluentui/react-components 尽量保持一致，样式也使用 css-in-js，我已经把 @griffel/react 转录成一个vue生态的lib，名字叫 griffel-vue。我们接下来一个一个的实现组件。

现在我们开始实现Button 组件，我给你 @fluentui/react-components Button 组件的源码，你帮我转换成 Vue3生态的组件。

等我给你react 生态的button 代码后，你再给我输出 vue3 tsx 的实现版本

上一个转录的文件，我们引入了 renderButtom.tsx依赖，现在我们开始转换 renderButtom.tsx，它的源码是这样的：```  ```, 请开始转换

这个Button.types.ts文件的代码：``` ```, 请转成vue 组件适用的

packages/react-components/react-button/library/src/contexts/ButtonContext.ts

使用 tsx 写组件，使用 css-in-js griffel-vue 组织组件的样式，组件库采用 微软的 fluentui design 设计体系。接口兼容 @flutent/react-components。组件库使用histoire 作为文案和测试方案，使用vitest 


我注意到Input 依赖 Field 和 Label  组件，Label  组件已经实现，现在我们的优先实现Field。现在分3步， 1、根据参考组件库 设计接口和实现方式 2 、实现组件，并完成 story 以及单元测试。 3、更新组件相关的文档。 另外 组件的目录结构参考Button，组件实现后要跑过单元测试和storybook的测试，确保组件的功能和文档都正确。

我仔细考虑了一下，现在组件库css 样式的方案，感觉食欲哦那个 css in js 的方案不太好，ai videcoding 下，使用css var 来实现 fluent design system 的主题和样式会更好一些，css var 可以更方便地实现主题切换和样式的复用，同时也能保持组件库的性能和可维护性。我们可以在组件库中定义一套 css var 来管理颜色、字体、间距等设计系统的核心元素，然后在组件中使用这些 css var 来实现样式，这样不仅能保持组件的一致性，还能让用户更容易地自定义和扩展组件库的样式。我们开始设计一个基于 css var 实现 @fluentui/react-components 中 fluent design system 的主题和样式的方案和实现计划